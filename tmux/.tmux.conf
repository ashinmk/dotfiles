# Vim settings
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi 'C-v' send -X rectangle-toggle  # Begin selection in copy mode.
setw -g mode-keys vi

set-option -g prefix C-s

bind-key C-s send-prefix
unbind-key C-b

set-option -g mouse on
set -s escape-time 0

# Related to splitting windows
bind-key - split-window -v
bind-key / split-window -h
bind-key \\ split-window -h

#bind-key -n C-u neww 'session=$(tmux ls|fzf); session_name=${session%%:*}; tmux switch-client -t "$session_name";'
#bind-key -n C-u run-shell "ls | fzf"
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

#bind-key -T copy-mode-vi 'C-h' select-pane -L
#bind-key -T copy-mode-vi 'C-j' select-pane -D
#bind-key -T copy-mode-vi 'C-k' select-pane -U
#bind-key -T copy-mode-vi 'C-l' select-pane -R
#bind-key -T copy-mode-vi 'C-\' select-pane -l

#bind-key -n C-h select-pane -L
#bind-key -n C-l select-pane -R
#bind-key -n C-j select-pane -D
#bind-key -n C-k select-pane -U

set-option -g base-index 1
set-option -g renumber-windows on

bind C-l send-keys -R \; send-keys C-l  \; clear-history
#\; clear-history
#bind-key -n C-o neww "tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse | xargs tmux switch-client -t"
bind-key -n C-e neww "tmux switch-client -t \"`tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse`\""


#bind-key \ split-window -h

#set-option -g default-terminal "screen-256color"
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"

set -g status-bg '#666666'
set -g status-fg '#aaaaaa'
set -g history-limit 10000

bind-key k run-shell 'tmux switch-client -n \; kill-session -t "$(tmux display-message -p "#S")" || tmux kill-session'

set-option -g status-left-length 50

bind -n S-Left resize-pane -L 2
bind -n S-Right resize-pane -R 2
bind -n S-Down resize-pane -D 2
bind -n S-Up resize-pane -U 2

bind l switch-client -l
bind p display-popup

bind-key s split-window -h "../he.sh; sleep 3"
bind-key t split-window -h "./d < input.txt > output.txt"
bind-key b split-window -h "g++ --std=c++17 d.cpp -o d || sleep 10"
